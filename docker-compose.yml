services:
  app:
    build: .
    container_name: reviewpilot
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./reviewpilot.db:/app/reviewpilot.db
    restart: unless-stopped
